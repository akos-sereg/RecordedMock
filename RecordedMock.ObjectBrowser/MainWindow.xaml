<Window x:Class="RecordedMock.ObjectBrowser.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:models="clr-namespace:RecordedMock.ObjectBrowser.Model"
        Title="MainWindow" Height="679.889" Width="1186.525">
    <Grid>
        <Label Content="Object File: " HorizontalAlignment="Left" Margin="10,10,0,0" VerticalAlignment="Top"/>
        <TextBox x:Name="objectFilePath" HorizontalAlignment="Left" Height="23" Margin="87,10,0,0" TextWrapping="Wrap" Text="" VerticalAlignment="Top" Width="302"/>
        <Button Content="Open ..." HorizontalAlignment="Left" Margin="405,10,0,0" VerticalAlignment="Top" Width="75" Click="Button_Click_1"/>
        
        <TabControl Margin="10,41,0,0">
            <TabItem Header="Captured Requests">
                <Grid Background="#FFE5E5E5" Margin="10,4,8,10">
                    <DataGrid x:Name="objectGrid" IsReadOnly="True" ColumnWidth="*" AutoGenerateColumns="False" CanUserResizeColumns="True" HorizontalAlignment="Stretch" Margin="10,10,10,330" VerticalAlignment="Stretch" HorizontalGridLinesBrush="#FFF1F1F1" VerticalGridLinesBrush="#FFF0F0F0" BorderThickness="1" SelectionMode="Single" SelectionChanged="objectGrid_SelectionChanged">
                        <DataGrid.Columns>
                            <DataGridTextColumn Binding="{Binding RecordedAt}" ClipboardContentBinding="{x:Null}" Header="Recorded At"/>
                            <DataGridTextColumn Binding="{Binding RequestUri}" ClipboardContentBinding="{x:Null}" Header="Details"/>
                        </DataGrid.Columns>
                    </DataGrid>
                    <TreeView Name="objectTreeView" Height="315" Margin="10,0,10,10" VerticalAlignment="Bottom" BorderThickness="0">
                        <TreeView.Resources>
                            <HierarchicalDataTemplate DataType="{x:Type models:ObjectNode}" ItemsSource="{Binding Path=Children}">
                                <TreeViewItem>
                                    <TreeViewItem.Header>
                                        <StackPanel Orientation="Horizontal" Margin="-10,0,0,0">
                                            <TextBlock Text="{Binding Path=Name}"/>
                                            <TextBlock Text=" : "/>
                                            <TextBlock Text="{Binding Path=Value}"/>
                                        </StackPanel>
                                    </TreeViewItem.Header>
                                </TreeViewItem>
                            </HierarchicalDataTemplate>
                        </TreeView.Resources>
                    </TreeView>
                </Grid>
            </TabItem>
            <TabItem Header="Captured Invocations on implementation: N/A">
                <Grid Background="#FFE5E5E5"/>
            </TabItem>
        </TabControl>

    </Grid>
</Window>
